services:
  - type: web
    name: plan-regulador
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn scripts.server:app --bind 0.0.0.0:$PORT --workers 2 --threads 4 --timeout 60
